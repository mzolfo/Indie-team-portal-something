using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CheckIfPositionisClearToDrop : MonoBehaviour
{
    /// <summary>
    /// this script is placed in the player drop position. It will check the default position to see if it is outside of the room,
    /// if it is then it will start with the nearest position into the room that is one clearance length away from the wall. 
    /// </summary>

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
